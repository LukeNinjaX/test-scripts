#!/usr/bin/env ts-node --dir ./
import { Command } from '@oclif/core';
export default class Init extends Command {
    static description: string;
    static flags: {
        dir: import("@oclif/core/lib/interfaces").OptionFlag<string, import("@oclif/core/lib/interfaces/parser").CustomOptions>;
    };
    run(): Promise<void>;
    ensureReadme(dir: string): void;
    ensureTestsDirectory(dir: string): void;
    ensureAsconfigJson(dir: string): void;
    ensureProjectConfigJson(dir: string): void;
    ensureTsconfigJson(rootDir: string): void;
    ensureContractDirectory(dir: string): void;
    ensureAssemblyDirectory(dir: string): void;
    ensureAspectDirectory(dir: string): void;
    ensureScriptDirectory(dir: string): void;
    ensurePackageJson(dir: string): void;
}
